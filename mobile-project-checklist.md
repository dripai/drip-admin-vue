# 移动端项目实施清单

本清单用于指导 AI 按移动端提示词文档开发轻量移动端入口框架，并在开发过程中持续更新状态。

状态只允许使用：
- 待开始
- 进行中
- 已完成

执行规则：
- 每次开发前必须先读取本清单。
- 每次只推进少量明确任务，优先完成 P0。
- 功能未完成或未验证，不允许标记为已完成。
- 每次开发结束前必须更新本清单状态。
- 每次提交前必须检查本清单是否同步。

## 0. 项目初始化

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 创建 uni-app 项目骨架 | 项目可以启动 |
| 已完成 | 配置 Vue 3 | Composition API 可用 |
| 已完成 | 配置 TypeScript | 类型检查可用 |
| 已完成 | 配置 Vite | 开发服务可启动 |
| 已完成 | 配置 Vant 4 | 移动端组件可渲染 |
| 已完成 | 配置 Pinia | store 可用 |
| 已完成 | 配置 Axios 请求封装 | 可调用后端接口 |
| 已完成 | 配置 Sass / SCSS | 全局样式和页面样式可用 |

## 1. 请求与状态 P0

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现 Axios request 实例 | 统一 baseURL、timeout、headers |
| 已完成 | 实现 token 注入 | 请求自动携带 token |
| 已完成 | 实现 401 处理 | 清理登录态并跳转登录页 |
| 已完成 | 实现 403 处理 | 显示无权限提示 |
| 已完成 | 实现业务错误提示 | 显示后端 message |
| 已完成 | 实现 authStore | 保存 token、expireAt、idleTimeout、maxSessionDuration、deviceType |
| 已完成 | 实现 userStore | 保存用户、角色、菜单树、权限码 |
| 已完成 | 实现 featureStore | 管理九宫格入口配置和过滤 |

## 2. 认证 P0

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现登录页 | 用户名、密码、登录按钮可用 |
| 已完成 | 登录自动携带 `deviceType` | 不展示设备类型选择框 |
| 已完成 | 实现 `useDeviceType` | deviceType 由运行端自动设置或客户端固定写入 |
| 已完成 | 登录成功后保存会话字段 | token、expireAt、idleTimeout、maxSessionDuration、deviceType 已持久化 |
| 已完成 | 登录成功后调用 `GET /api/auth/me` | 获取用户、角色、菜单树、权限码 |
| 已完成 | 实现退出登录 | 调用后端退出接口并清理本地状态 |
| 已完成 | token 失效处理 | 401 后跳转登录页 |

## 3. 首页九宫格 P0

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现首页 | 第一屏只展示简洁顶部栏和九宫格 |
| 已完成 | 实现九宫格入口配置 | `features/entries.ts` 集中维护 |
| 已完成 | 实现 FeatureGrid 组件 | 每行 3 个入口 |
| 已完成 | 实现权限码过滤 | 无权限入口不显示 |
| 已完成 | 实现排序 | 按 sort 升序显示 |
| 已完成 | 实现入口跳转 | 点击入口跳转 path |
| 已完成 | 实现空状态 | 无可用入口时展示简短空状态 |
| 已完成 | 实现功能占位页 | 暂未实现入口显示简短提示 |
| 已完成 | 首页不展示无用内容 | 无统计卡片、欢迎大图、banner |

## 4. 通用能力 P0

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现 EmptyState 组件 | 可复用空状态 |
| 已完成 | 实现 loading 状态 | 登录和请求中有明确反馈 |
| 已完成 | 实现错误提示 | 业务错误显示后端 message |
| 已完成 | 实现本地存储工具 | token 和会话字段可持久化 |
| 已完成 | 实现权限工具 | 根据 permissionCode 判断入口可见 |
| 已完成 | 实现路由守卫 | 未登录访问首页跳转登录页 |

## 5. 联调验收

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 移动端项目启动成功 | 本地 H5 页面可访问 |
| 待开始 | 登录成功 | 进入首页 |
| 待开始 | 登录失败提示正确 | 显示后端 message |
| 待开始 | 登录请求自动携带 deviceType | 后端在线用户可看到原始 deviceType |
| 待开始 | `GET /api/auth/me` 调用成功 | 返回用户、角色、菜单树、权限码 |
| 待开始 | 九宫格按权限显示 | 无权限入口不显示 |
| 待开始 | 九宫格入口可跳转 | 点击后进入对应页面或占位页 |
| 待开始 | 退出登录成功 | 清理状态并回到登录页 |
| 待开始 | token 失效后跳转登录 | 401 可正确处理 |

