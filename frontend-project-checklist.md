# PC 前端项目实施清单

本清单用于指导 AI 按 PC 前端提示词文档开发后台管理系统，并在开发过程中持续更新状态。

状态只允许使用：
- 待开始
- 进行中
- 已完成

执行规则：
- 每次开发前必须先读取本清单。
- 每次只推进少量明确任务，优先完成 P0。
- 功能未完成或未验证，不允许标记为已完成。
- 每次开发结束前必须更新本清单状态。
- 每次提交前必须检查本清单是否同步。

## 0. 项目初始化

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 创建 Vue 3 + TypeScript + Vite 项目骨架 | 项目可以启动 |
| 已完成 | 配置 Ant Design Vue | 基础组件可正常渲染 |
| 已完成 | 配置 Pinia | store 可用 |
| 已完成 | 配置 Vue Router | 路由可用 |
| 已完成 | 配置 Axios 请求封装 | 可调用后端接口 |
| 已完成 | 配置 Sass / SCSS | 全局样式和组件样式可用 |
| 已完成 | 配置 ESLint / Prettier | 代码格式检查可用 |

## 1. 请求与状态 P0

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现 Axios request 实例 | 统一 baseURL、timeout、headers |
| 已完成 | 实现 token 注入 | 请求自动携带 token |
| 已完成 | 实现 401 处理 | 清理登录态并跳转登录页 |
| 已完成 | 实现 403 处理 | 显示无权限提示 |
| 已完成 | 实现业务错误提示 | 显示后端 message |
| 已完成 | 实现 authStore | 保存 token、expireAt、idleTimeout、maxSessionDuration、deviceType |
| 已完成 | 实现 userStore | 保存用户、角色、菜单树、权限码 |
| 已完成 | 实现 permissionStore | 动态路由和权限判断可用 |
| 已完成 | 实现 dictStore | 字典缓存和刷新可用 |

## 2. 认证与布局 P0

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现登录页 | 用户名、密码、登录按钮可用 |
| 已完成 | 登录自动携带 `deviceType` | 不展示设备类型选择框 |
| 已完成 | 登录成功后调用 `GET /api/auth/me` | 获取用户、角色、菜单树、权限码 |
| 已完成 | 实现退出登录 | 调用后端退出接口并清理状态 |
| 已完成 | 实现主布局 | 左侧菜单、顶部栏、面包屑、内容区可用 |
| 已完成 | 实现动态菜单 | 根据菜单树生成左侧菜单 |
| 已完成 | 实现动态路由 | 根据菜单树生成路由 |
| 已完成 | 实现按钮权限 | 根据 permissionCode 控制按钮显示 |
| 已完成 | 实现 IconRenderer | 菜单图标按 icon key 渲染 |

## 3. 通用组件 P0

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现 PageContainer | 统一页面间距、loading、错误状态 |
| 已完成 | 实现 SearchForm | 查询、重置、展开收起可用 |
| 已完成 | 实现 DataTable | 分页、loading、空状态可用 |
| 已完成 | 实现 FormModal / FormDrawer | 新增编辑复用 |
| 已完成 | 实现 PermissionButton | 根据权限码显示或隐藏 |
| 已完成 | 实现 ConfirmAction | 危险操作二次确认 |
| 已完成 | 实现 StatusSwitch / StatusTag | 启用禁用状态统一 |
| 已完成 | 实现 DictSelect / DictTag | 字典下拉和标签渲染 |
| 已完成 | 实现 IconSelect | 菜单图标选择可用 |
| 已完成 | 实现 useTable | 分页查询、刷新、重置可用 |

## 4. P0 页面

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现用户管理页 | 查询、分页、新增、编辑、删除、启用禁用、重置密码、分配角色可用 |
| 已完成 | 实现角色管理页 | 查询、分页、新增、编辑、删除、启用禁用可用 |
| 已完成 | 实现角色授权弹窗 | 菜单和按钮权限可配置 |
| 已完成 | 实现菜单权限页 | 目录、菜单、按钮权限可维护 |
| 已完成 | 实现登录日志页 | 分页查询、详情查看可用 |
| 已完成 | 实现业务操作日志页 | 分页查询、详情查看可用，表格不展示 IP 和 userAgent |

## 5. P1 页面与能力

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | 实现部门管理页 | 部门树和增删改可用 |
| 已完成 | 实现字典管理页 | 字典类型和字典项可维护 |
| 已完成 | 实现系统配置页 | 配置可维护，敏感配置脱敏 |
| 已完成 | 实现在线用户页 | 展示 deviceType、IP、userAgent、登录时间 |
| 已完成 | 实现强制下线 | 二次确认后可执行 |
| 已完成 | 实现定时任务管理页 | 任务可增删改查、启用禁用、手动执行 |
| 已完成 | 实现数据库备份恢复页 | 备份、下载、恢复、删除可操作 |
| 已完成 | 实现 FileUpload 预留组件 | 类型、大小、数量限制可配置 |
| 已完成 | 实现 ImportExportActions 预留组件 | 模板下载、导入、导出入口可配置 |
| 已完成 | 实现个人偏好本地持久化 | 不依赖后端接口 |

## 6. 联调验收

| 状态 | 任务 | 验证标准 |
|---|---|---|
| 已完成 | PC 前端启动成功 | 本地页面可访问 |
| 已完成 | 登录成功 | 进入后台首页 |
| 已完成 | 登录失败提示正确 | 显示后端 message |
| 已完成 | token 失效后跳转登录页 | 401 可正确处理 |
| 已完成 | 菜单按权限渲染 | 无权限菜单不显示 |
| 已完成 | 按钮按权限显示 | 无权限按钮不显示 |
| 已完成 | 用户管理主流程可用 | 增删改查可完成 |
| 已完成 | 角色授权后权限生效 | 重新登录后菜单和按钮变化 |
| 已完成 | 登录日志可查询 | 页面数据来自后端 |
| 已完成 | 业务操作日志可查询 | 页面数据来自后端 |

